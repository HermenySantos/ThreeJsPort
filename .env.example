# Optional. Used only for the public GitHub contributions/stats widget.
# A fine-grained token with public read access is enough — no repo scope needed.
# Leave unset to fall back to the unauthenticated public API.
# Create a token at: https://github.com/settings/tokens
GITHUB_TOKEN=your_github_token_here
